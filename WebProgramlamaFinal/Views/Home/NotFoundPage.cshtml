@{
    Layout = "_Layout";
    ViewData["Title"] = "404 - Sayfa Bulunamadý";
}

<div class="container text-center" style="margin-top: 10%; margin-bottom: 10%;">
    <div class="row justify-content-center">
        <div class="col-md-6">
            @* 404 Ýkonu ve Baþlýk *@
            <div class="display-1 text-secondary mb-4">
                <i class="bi bi-search-heart" style="font-size: 5rem;"></i>
                <span class="fw-bold">404</span>
            </div>

            <h2 class="mb-3 fw-light">Aradýðýnýz Sayfayý Bulamadýk!</h2>

            <p class="text-muted mb-5">
                Görünüþe göre aradýðýnýz ürün, kullanýcý veya sayfa taþýnmýþ ya da hiç var olmamýþ olabilir.
                Lütfen adresi kontrol edin veya aþaðýdaki butonu kullanarak ana sayfaya dönün.
            </p>

            @* Yönlendirme Butonlarý *@
            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg px-4 gap-3 shadow">
                    <i class="bi bi-house-door-fill"></i> Ana Sayfaya Dön
                </a>
                <a href="javascript:history.back()" class="btn btn-outline-secondary btn-lg px-4">
                    <i class="bi bi-arrow-left"></i> Geri Git
                </a>
            </div>
        </div>
    </div>
</div>

@* Küçük bir animasyon dokunuþu *@
<style>
    .display-1 i {
        display: inline-block;
        animation: float 3s ease-in-out infinite;
    }

    @@keyframes float {
        0% {
            transform: translateY(0px);
        }

        50% {
            transform: translateY(-20px);
        }

        100% {
            transform: translateY(0px);
        }
    }
</style>